#!/usr/bin/env node

const {join} = require('path')
const devMode = require('fs').existsSync(join(__dirname, '..', 'src'))

if (devMode) {
  require('ts-node').register({project: join(__dirname, '..', 'tsconfig.json')})
  require('../src/bin').run()
} else {
  require('../lib/bin').run()
}
